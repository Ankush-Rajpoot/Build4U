// This file is no longer needed with the current upload implementation
// and can be deleted to avoid confusion.
// If you need to keep it for other purposes, ensure it imports the
// configured instance from ../config/cloudinary.js instead of re-configuring.
//   api_key: process.env.CLOUDINARY_API_KEY,
//   api_secret: process.env.CLOUDINARY_API_SECRET
// });

// export const uploadToCloudinary = async (base64File) => {
//   return await cloudinary.v2.uploader.upload(base64File, {
//     folder: 'service-request-uploads'
//   });
// };
